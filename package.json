{
  "name": "flux-krea-ai",
  "version": "1.0.0",
  "description": "AI Image Generator with FLUX model",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && cp index.html public/ && cp showcase.html public/ && cp test_api.html public/ && cp pricing.html public/ && cp subscription-success.html public/ && echo 'Static site ready for deployment'",
    "start": "vercel dev",
    "test-api": "node test_local_api.js",
    "check-env": "node check_env.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.53.0",
    "dotenv": "^16.3.1",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
